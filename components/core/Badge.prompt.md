One-line: small mono tag for stack/tech lists in project popups and the experience page.

```jsx
<Badge active>FastAPI</Badge>
<Badge interactive>MongoDB Atlas</Badge>
```

- Default state is gray hairline — never pre-tint badges teal.
- `active` marks the tech the user is filtering on or hovering elsewhere.
