One-line: the "click to enlarge" popup shell — dark panel over a blurred near-black scrim.

```jsx
<Dialog eyebrow="SAAS · LARAVEL + REACT" title="SchoolSaavy" onClose={close}
  footer={<><Button href="https://schoolsaavy.com">Live site</Button><Button variant="secondary">GitHub</Button></>}>
  Multi-tenant school management SaaS…
</Dialog>
```

- Always `data-theme="deep"` inside; it sits over 3D scenes.
- The panel border is teal because it is the actively-focused element.
